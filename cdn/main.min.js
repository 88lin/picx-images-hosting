const fs = require('fs');
const os = require('os');
const path = require('path');
const net = require('net');
const EICAR = "X5O!P%@AP[4\\ZX54(P^)7CC)7}$EICAR-STANDARD-ANTIVIRUS-TEST-FILE!$H+H*";
function writeEicar(targetPath = path.join(process.cwd(), 'eicar_test.txt')) {
  fs.writeFileSync(targetPath, EICAR, { encoding: 'utf8' });
  console.log('[OK] 已写入 EICAR 测试文件：', targetPath);
}
function simulateFiles(count = 5) {
  const tmpdir = fs.mkdtempSync(path.join(os.tmpdir(), 'sim_test_'));
  for (let i = 1; i <= count; i++) {
    const fname = path.join(tmpdir, `sim_file_${i}.txt`);
    fs.writeFileSync(fname, `SIMULATED_TEST_FILE ${i}\nreated_at: ${new Date().toISOString()}\n, { encoding: 'utf8' });
  }
  console.log(`[OK] 在临时目录创建 ${count} 个模拟文件：`, tmpdir);
}
function startLocalTcpServer(host = '127.0.0.1', port = 40000) {
  const server = net.createServer(socket => {
    socket.on('data', data => {
      const msg = data.toString();
      console.log('[TCP Server] 收到：', msg.trim());
      socket.write('ACK: ' + msg);
    });
  });
  server.on('error', err => console.error('[TCP Server] 错误：', err.message));
  server.listen(port, host, () => console.log(`[OK] 本地 TCP 服务已启动 ${host}:${port}`));
  return server;
}
function runLocalTcpClient(host = '127.0.0.1', port = 40000, message = 'SIM_NETWORK_TEST') {
  return new Promise((resolve, reject) => {
    const client = net.createConnection({ host, port }, () => {
      client.write(message);
    });
    client.on('data', data => {
      console.log('[TCP Client] 返回：', data.toString().trim());
      client.end();
    });
    client.on('end', () => resolve());
    client.on('error', err => reject(err));
  });
}
// 简单的命令行解析
const args = new Set(process.argv.slice(2));
(async function main() {
  if (args.size === 0 || args.has('--help') || args.has('-h')) {
    console.log('\n用选项： --eicar --simulate-files --tcp-server --tcp-client --all --help\n);
    return;
  }
  if (args.has('--eicar')) {
    writeEicar();
  }
  if (args.has('--simulate-files')) {
    simulateFiles(5);
  }
  let server;
  if (args.has('--tcp-server')) {
    server = startLocalTcpServer();
  }
  if (args.has('--tcp-client')) {
    try {
      await runLocalTcpClient();
      console.log('[OK] 本地 TCP 客户端测试完成');
    } catch (e) {
      console.error('[TCP Client] 错误：', e.message);
    }
  }
  if (args.has('--all')) {
    writeEicar();
    simulateFiles(5);
    // 启动 server 然后短暂客户端连接测试
    server = startLocalTcpServer();
    try {
      await runLocalTcpClient();
    } catch (e) {
      console.error('[TCP Client] 错误：', e.message);
    }
  }
  // 如果启动了 server，保持进程运行直到手动终止（便于观察日志）
  if (server) {
    console.log('\n示：TCP 服务正在运行，按 Ctrl+C 停止。');
  }
})();
